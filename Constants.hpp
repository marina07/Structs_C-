#ifndef CONSTANTS_H
#define CONSTANTS_H

//Field is used as a parameter in the function fieldLength, which determines the length/size
//of a field (struct AdShown)
enum Field {
	ID,
	DESCRIPTION,
	VALUE,
	CLICK,
	SHOWN
};


#endif
